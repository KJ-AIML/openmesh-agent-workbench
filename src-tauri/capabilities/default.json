{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Openmesh v0.3.0 — Phase 3 adds folder picker dialog.",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open"
  ]
}
