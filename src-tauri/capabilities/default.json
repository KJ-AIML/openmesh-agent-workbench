{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Openmesh v0.3.0 — Full file-based storage.",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open",
    "fs:default",
    "fs:allow-read-file",
    "fs:allow-write-file",
    "fs:allow-read-dir",
    "fs:allow-mkdir",
    "fs:allow-remove",
    "fs:allow-exists",
    "fs:scope-home",
    "fs:scope-home-recursive"
  ],
  "_comment": "Broad filesystem permissions required because projects can be stored anywhere on disk. App only writes to .openmesh/ directories within project folders and ~/.openmesh/ for global data. User source files are never modified or deleted."
}
